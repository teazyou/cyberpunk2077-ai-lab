---
name: uninstall
description: Uninstall (fully delete) a Cyberpunk 2077 mod. Use when the user runs /uninstall with a mod name/title or Nexus URL.
argument-hint: <url | mod name>
---
# /uninstall <url | mod name>

Argument = a mod name/title or Nexus URL.

`mod-manager.md` (project root) is the standalone source of truth — read ONLY that file; do not read any other file.

1. Locate the mod's entry under `## Mod Manager Data`.
2. Perform the UNINSTALL procedure defined in `mod-manager.md` (Procedures → UNINSTALL) exactly. Do not restate or invent steps.
